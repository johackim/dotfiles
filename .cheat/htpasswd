# Generate htpasswd

```
# .htaccess for apache
AuthType Basic
AuthName "Protected"
AuthUserFile "<PATH>"
Require valid-user
```

htpasswd -c .htpasswd <user>

# Other way to protect html (client side)
https://github.com/robinmoisson/staticrypt
