# Create a key
gpg --gen-key

# List gpg keys
gpg --list-secret-keys --keyid-format LONG

# Delete key
gpg --delete-keys <KEY>

# Change password
gpg --edit-key Your-Key-ID-Here
gpg> passwd
gpg> save
