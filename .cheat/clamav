# Update antivirus database
sudo freshclam
sudo clamd

# Scan recursive system
sudo clamscan -r /

# Show only infected files
clamscan -i -r /

# Scan file or directory
sudo clamscan <file|directory>
