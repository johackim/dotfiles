# General
auto-reload yes
refresh-on-startup yes
confirm-exit yes
reload-time 30
browser chromium
player vlc
article-sort-order date-asc
error-log "~/.newsbeuter/error.log"
save-path "~/Notes/Bookmarks"
goto-next-feed no
show-read-feeds no

# Colors
color listnormal          default default 
color listfocus           black white
color listfocus_unread    black white bold
color info                default cyan bold

# Keybinding
bind-key k up
bind-key j down
bind-key q quit
unbind-key l
bind-key l open
bind-key m toggle-show-read-feeds

# Filters
ignore-article "http://feeds.feedburner.com/SpiritOfMetalWebzine/fr?format=xml" "title !~ \"chronique\""

# Notification
# notify-always yes
# notify-format "newsbeuter: %n unread articles within %f unread feeds"
# notify-program notify-send

